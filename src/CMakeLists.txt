cmake_minimum_required(VERSION 3.9.1)
PROJECT(p33)
FIND_PACKAGE(CUDA QUIET REQUIRED)
SET(CUDA_NVCC_FLAGS ${CUDA_NVCC_FLAGS};-std=c++11;) 
INCLUDE_DIRECTORIES( )
LINK_DIRECTORIES( )
CUDA_ADD_EXECUTABLE( p33 p33.cu)
